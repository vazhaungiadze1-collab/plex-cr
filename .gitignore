node_modules/
.firebase/
firebase-debug.log
firestore-debug.log
.DS_Store

# Never commit the Firebase service-account key — it goes into the
# FIREBASE_SERVICE_ACCOUNT GitHub Actions secret instead (see README.md).
*serviceAccount*.json
*firebase-adminsdk*.json
service-account*.json
